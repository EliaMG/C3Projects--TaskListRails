<%= render "shared/header" %>

<main>
  <table>
    <tr>
      <th>Name</th>
      <th>Uncompleted Tasks</th>
    </tr>

    <% @all_people.each do |person| %>
    <tr>
      <td> <%= "#{person.id}" %> </td>
      <td> <%= "#{person.name}" %></td>
    </tr>
    <% end %>
  </table>
</main>

<%= render "shared/footer" %>
